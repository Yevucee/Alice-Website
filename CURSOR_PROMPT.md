# Paste this into Cursor

I have a finished static website in this folder. **Do not redesign, restyle, or rewrite it.** The look must stay byte-for-byte identical to `index.html` as provided.

Your job is only to:

1. **Leave `index.html` exactly as it is.** Do not touch the HTML, CSS, copy, colors, fonts, image URLs, or layout. If you think something could be "improved," do not change it — keep it identical.
2. Add a `.gitignore` for a basic static site (ignore `.DS_Store`, `node_modules/`, `.vscode/`).
3. Initialize a git repository, stage all files, and make one commit: `Initial commit: Alice the Time Bender website`.
4. Remote is already set up: **https://github.com/Yevucee/Alice-Website** — push changes to `main` when ready.
5. To go live, enable **GitHub Pages**: Settings → Pages → deploy from `main`, root folder (`/`).

Constraints:
- This is a plain HTML/CSS site. **Do not** add React, a bundler, Tailwind, a package.json, or any build step.
- Do not move code into separate `.css` / `.js` files — everything intentionally lives inline in `index.html`.
- Verify the page renders identically by opening it in a browser before committing.

If you need to authenticate with GitHub, prompt me to do it — don't attempt to store or guess any credentials.
